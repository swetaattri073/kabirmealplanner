# Copy this to .env or pass URL on the CLI:
#   npm run configure -- https://your-domain
# Prefills the first-launch server field on Android + iOS.
SERVER_URL=https://REPLACE_WITH_YOUR_LITTLEBOWL_URL
